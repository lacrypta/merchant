/**
 * Test stub for the `server-only` package.
 *
 * Importing the real one outside a Next build throws, which would make every
 * module under src/lib/server untestable. Aliased in vitest.config.mts.
 */
export {}
